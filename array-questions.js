var arry = [7, 57, 8, 54, 5, 8];
function reversearray(arr) {
    for (var i = arr.length - 1; i >= 0; i--) {
        var arrs = [];
        arrs.push(arr[i]);
        console.log(arrs);
    }
}
reversearray(arry);
